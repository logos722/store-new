# Исправление проблемы ISR при билде

## 🔴 Проблема

При деплое приложения в Docker контейнере на продакшене главная страница отображает "Каталоги не найдены", хотя локально все работает корректно.

### Причина

Во время выполнения `npm run build` в Docker контейнере:

1. **Next.js пытается выполнить ISR pre-rendering** главной страницы
2. **API сервер недоступен** во время билда (контейнер с backend не запущен)
3. **Все fetch запросы падают** с ошибкой `ECONNREFUSED ::1:5000`
4. **Функция `fetchCatalogShowcase` возвращает `null`** для каждой категории
5. **Массив витрин фильтруется** и становится пустым `[]`
6. **Страница кэшируется с пустым состоянием** и деплоится на прод

### Ошибки в логах билда

```
Error fetching catalog showcase for 936a16d1-79a7-11e6-ab15-d017c2d57ada: TypeError: fetch failed
  cause: Error: connect ECONNREFUSED ::1:5000
    errno: -111,
    code: 'ECONNREFUSED',
    syscall: 'connect',
    address: '::1',
    port: 5000
```

## ✅ Решение 1: Dynamic Rendering (Рекомендуется)

**Самое простое и надежное решение** - использовать динамический рендеринг вместо статического.

### Что изменили

В `src/app/page.tsx` добавили:

```typescript
// Используем динамический рендеринг вместо статического
// Это решает проблему с недоступным API во время билда
export const dynamic = 'force-dynamic';
export const revalidate = 3600; // Revalidate каждый час
```

### Как это работает

- **Во время билда**: Next.js НЕ пытается pre-render страницу
- **На продакшене**: Страница рендерится при каждом запросе на сервере
- **ISR все еще работает**: `revalidate: 3600` кэширует результат на 1 час
- **API доступен**: Во время рантайма backend уже запущен

### Преимущества

✅ Билд проходит без ошибок  
✅ Не требует изменений в Docker/CI/CD  
✅ Актуальные данные на проде  
✅ ISR кэширование работает  
✅ SEO не страдает (SSR все еще работает)

### Недостатки

⚠️ Первый запрос может быть немного медленнее (но кэшируется на 1 час)

---

## 🔧 Решение 2: Fallback данные

Если критически важен pre-rendering во время билда, можно добавить fallback логику.

### Изменения в `src/shared/api/fetchCatalogShowcases.ts`

```typescript
export async function fetchCatalogShowcase(
  categoryId: string,
  limit: number = 4,
): Promise<CatalogShowcase | null> {
  const apiUrl = getApiUrl();
  const fullUrl = `${apiUrl}/api/catalog/${encodeURIComponent(categoryId)}?page=1&limit=${limit}`;

  try {
    const response = await fetch(fullUrl, {
      next: { revalidate: 3600 },
      // Добавляем таймаут для билда
      signal: AbortSignal.timeout(5000),
    });

    if (!response.ok) {
      return createFallbackShowcase(categoryId);
    }

    const data: CatalogApiResponse = await response.json();
    // ... остальной код
  } catch (error) {
    console.error(`Error fetching catalog ${categoryId}, using fallback`);
    return createFallbackShowcase(categoryId);
  }
}

// Создаем fallback витрину с информацией из констант
function createFallbackShowcase(categoryId: string): CatalogShowcase {
  const catalogInfo = CatalogInfo[categoryId as CatalogId];
  
  return {
    categoryId,
    title: catalogInfo?.title || 'Каталог',
    description: catalogInfo?.description || '',
    slug: catalogInfo?.slug || categoryId,
    imageUrl: catalogInfo?.imageUrl,
    products: [], // Пустой массив, будет заполнен на клиенте
    totalProducts: 0,
  };
}
```

### Преимущества

✅ Билд проходит успешно  
✅ Страница всегда показывает что-то (не пустое состояние)  
✅ Pre-rendering работает

### Недостатки

⚠️ Нужно поддерживать fallback данные  
⚠️ Может показывать неактуальную информацию

---

## 🐳 Решение 3: Настройка Docker окружения

Можно настроить Docker так, чтобы API был доступен во время билда.

### Вариант 3.1: Multi-stage build с ожиданием API

```dockerfile
# Dockerfile
FROM node:18-alpine AS builder
WORKDIR /app

# Копируем файлы
COPY package*.json ./
RUN npm ci
COPY . .

# Устанавливаем переменные окружения для билда
ARG API_BASE_URL
ENV API_BASE_URL=${API_BASE_URL}

# Билд (API должен быть доступен)
RUN npm run build

FROM node:18-alpine AS runner
WORKDIR /app
COPY --from=builder /app ./
RUN npm ci --production

EXPOSE 3000
CMD ["npm", "run", "start"]
```

### Вариант 3.2: Docker Compose с зависимостями

```yaml
# docker-compose.yml
version: '3.8'

services:
  backend:
    image: your-backend:latest
    ports:
      - "5000:5000"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/health"]
      interval: 10s
      timeout: 5s
      retries: 5

  frontend:
    build:
      context: .
      args:
        API_BASE_URL: http://backend:5000
    depends_on:
      backend:
        condition: service_healthy
    ports:
      - "3000:3000"
```

### Преимущества

✅ ISR pre-rendering работает полностью  
✅ Оптимальная производительность

### Недостатки

⚠️ Сложнее настройка CI/CD  
⚠️ Увеличивает время билда  
⚠️ Требует доступный API во время билда

---

## 🔍 Диагностика

### Добавленные логи

В `fetchCatalogShowcases.ts` добавлены подробные логи для диагностики:

```typescript
console.log(`[fetchCatalogShowcase] Fetching: ${fullUrl}`);
console.log(`[fetchCatalogShowcase] Success: ${showcase.products.length} products`);
console.error(`[fetchCatalogShowcase] Error: ...`);
console.error(`[fetchCatalogShowcase] API URL was: ${fullUrl}`);
console.error(`[fetchCatalogShowcase] Environment: API_BASE_URL=...`);
```

### Как проверить логи

**В Docker:**
```bash
docker logs <container-name> 2>&1 | grep fetchCatalogShowcase
```

**В продакшене:**
Проверьте логи вашего хостинга (Vercel, Railway, etc.)

---

## 📊 Сравнение решений

| Критерий | Решение 1 (Dynamic) | Решение 2 (Fallback) | Решение 3 (Docker) |
|----------|---------------------|----------------------|--------------------|
| Простота | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐ |
| Скорость билда | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐ |
| Актуальность данных | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| SEO | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| Сложность поддержки | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐ |

---

## 🎯 Рекомендация

**Используйте Решение 1 (Dynamic Rendering)** - это оптимальный баланс между простотой и производительностью.

Если нужен более детальный контроль или у вас специфические требования к кэшированию, можно комбинировать решения 1 и 2.

---

## 📝 Чеклист

После применения исправлений:

- [ ] Убедитесь, что билд проходит без ошибок `ECONNREFUSED`
- [ ] Проверьте, что на проде отображаются каталоги
- [ ] Проверьте логи на предмет ошибок API
- [ ] Убедитесь, что SEO метаданные на месте
- [ ] Проверьте Core Web Vitals не ухудшились
- [ ] Проверьте, что ISR кэширование работает (второй запрос быстрее)

---

## 🔗 Связанные файлы

- `src/app/page.tsx` - главная страница с ISR
- `src/shared/api/fetchCatalogShowcases.ts` - API для загрузки витрин
- `src/shared/utils/getApiUrl.ts` - определение API URL
- `Dockerfile` - Docker конфигурация
- `next.config.js` - Next.js конфигурация

---

## ❓ FAQ

### В: Почему локально работает?

**О:** Локально вы запускаете `npm run dev`, который использует development сервер с полностью динамическим рендерингом. Проблема проявляется только при production билде (`npm run build`).

### В: Можно ли использовать Static Export?

**О:** Нет, static export (`output: 'export'`) не поддерживает ISR и динамические API роуты, которые используются в проекте.

### В: Как проверить, что ISR работает?

**О:** Сделайте два запроса на главную страницу с разницей в несколько секунд. Второй должен быть значительно быстрее (кэшированный). Проверьте заголовки ответа - должен быть `X-Nextjs-Cache: HIT`.

### В: Что делать, если проблема вернулась?

**О:** Проверьте:
1. Не удалены ли строки `export const dynamic = 'force-dynamic'`
2. Переменные окружения `API_BASE_URL` установлены правильно
3. Backend доступен на продакшене
4. Логи приложения на наличие ошибок API

---

**Дата создания:** 2025-11-18  
**Статус:** ✅ Исправлено

