# Объяснение Dockerfile

## 🎯 Оптимизации и решения

### 1. ✅ `npm ci --production` восстановлен

**Почему это важно:**

```dockerfile
# ❌ ПЛОХО: Копирование всех node_modules из builder
COPY --from=builder /app/node_modules ./node_modules

# ✅ ХОРОШО: Установка только production зависимостей
RUN npm ci --production
```

**Преимущества:**
- ⚡ **Размер образа меньше на ~100-200 MB** (без devDependencies)
- 🔒 **Безопаснее** (нет dev пакетов типа `eslint`, `typescript`, etc.)
- 📦 **Только runtime зависимости** (React, Next.js, etc.)

**Сравнение размеров:**

| Подход | node_modules | Docker Image |
|--------|--------------|--------------|
| Копирование всех | ~400 MB | ~550 MB |
| `--production` | ~200 MB | ~350 MB |
| **Экономия** | **~200 MB** | **~200 MB** |

---

### 2. ✅ ENV переменные убраны из Dockerfile

**Почему не нужны в Dockerfile:**

```dockerfile
# ❌ НЕ НУЖНО (если используете .env или docker-compose)
ARG API_BASE_URL
ENV API_BASE_URL=${API_BASE_URL}
```

**Причины:**

1. **Dynamic rendering** - билд не требует доступа к API
2. **Runtime переменные** - берутся из `.env` файла или `docker-compose.yml`
3. **Гибкость** - можно менять переменные без пересборки образа

**Где устанавливать переменные:**

#### Вариант 1: .env файл (рекомендуется)

```bash
# ~/project/.env
API_BASE_URL=http://backend:5000
NEXT_PUBLIC_API_BASE_URL=https://api.gelionaqua.ru
NEXT_PUBLIC_BASE_URL=https://gelionaqua.ru
```

```yaml
# docker-compose.yml
services:
  frontend:
    image: ghcr.io/logos722/store-new:latest
    env_file: .env  # ← Автоматически загружает переменные
```

#### Вариант 2: docker-compose.yml

```yaml
services:
  frontend:
    image: ghcr.io/logos722/store-new:latest
    environment:
      - API_BASE_URL=http://backend:5000
      - NEXT_PUBLIC_API_BASE_URL=https://api.gelionaqua.ru
```

#### Вариант 3: docker run

```bash
docker run -e API_BASE_URL=http://backend:5000 \
           -e NEXT_PUBLIC_API_BASE_URL=https://api.gelionaqua.ru \
           store-new
```

---

## 📊 Структура оптимизированного Dockerfile

```dockerfile
# ========================================
# Stage 1: Builder (сборка приложения)
# ========================================
FROM node:18-alpine AS builder

# Установка ВСЕ зависимостей (включая devDependencies)
RUN npm ci

# Билд приложения (TypeScript → JavaScript, SCSS → CSS, etc.)
RUN npm run build

# ========================================
# Stage 2: Runner (production runtime)
# ========================================
FROM node:18-alpine AS runner

# Установка ТОЛЬКО production зависимостей
RUN npm ci --production  # ← Убирает devDependencies

# Копирование только необходимых файлов
COPY --from=builder /app/.next ./.next
COPY --from=builder /app/public ./public

# Runtime переменные из .env или docker-compose
```

## 🔍 Что происходит во время сборки

### Builder Stage

```
1. npm ci
   ├── Устанавливает ВСЕ зависимости
   ├── typescript (для компиляции)
   ├── eslint (для проверки)
   ├── sass (для SCSS)
   └── next (для сборки)

2. npm run build
   ├── TypeScript → JavaScript
   ├── SCSS → CSS
   ├── Оптимизация изображений
   └── Генерация .next/ папки
```

### Runner Stage

```
1. npm ci --production
   ├── Устанавливает ТОЛЬКО production зависимости
   ├── react ✅
   ├── next ✅
   ├── typescript ❌ (не нужен в runtime)
   └── eslint ❌ (не нужен в runtime)

2. Копирование файлов
   ├── .next/ (скомпилированное приложение)
   ├── public/ (статические файлы)
   └── node_modules/ (только production)

3. Запуск
   └── npm run start (Next.js production server)
```

## ✅ Преимущества текущего подхода

### 1. Оптимальный размер

```
Builder stage:  ~800 MB (все зависимости + исходники)
              ↓
Runner stage:   ~350 MB (только runtime)
              ↓
Удаляется:      ~450 MB (devDependencies + исходники)
```

### 2. Быстрый деплой

- **Меньше образ** → быстрее pull с registry
- **Меньше слоев** → быстрее распаковка
- **Кэширование** → повторные деплои моментальные

### 3. Безопасность

- **Нет dev инструментов** в production
- **Нет исходников** TypeScript/SCSS
- **Только runtime код**

### 4. Гибкость

- **Переменные извне** → не нужен пересборка образа
- **Один образ** → разные окружения (staging, prod)
- **Легкая отладка** → можно менять переменные на лету

## 🚀 Как использовать

### Локальная сборка

```bash
# Билд (быстрый, без API)
docker build -t store-new .

# Запуск с переменными из .env файла
docker run --env-file .env -p 3000:3000 store-new
```

### Production деплой

```bash
# 1. CI/CD собирает образ и пушит в registry
docker build -t ghcr.io/logos722/store-new:latest .
docker push ghcr.io/logos722/store-new:latest

# 2. На сервере создайте .env файл
cat > .env << EOF
API_BASE_URL=http://backend:5000
NEXT_PUBLIC_API_BASE_URL=https://api.gelionaqua.ru
NEXT_PUBLIC_BASE_URL=https://gelionaqua.ru
EOF

# 3. Запустите с docker-compose
docker-compose up -d
```

## ❓ FAQ

### В: Почему не использовать ARG для передачи переменных при билде?

**О:** С dynamic rendering переменные не нужны во время билда. Все данные загружаются на runtime. ARG был бы нужен только для static export, который не поддерживает динамические API.

### В: Можно ли вернуть ARG/ENV в Dockerfile?

**О:** Да, если нужно:

```dockerfile
ARG API_BASE_URL=http://localhost:5000
ENV API_BASE_URL=${API_BASE_URL}
```

Но это необязательно и может привести к "запеченным" значениям в образ.

### В: Как проверить размер образа?

```bash
# До оптимизации
docker images store-new
# store-new  latest  550MB

# После оптимизации с npm ci --production
docker images store-new
# store-new  latest  350MB

# Экономия: ~200MB (36%)
```

### В: Что делать, если нужны разные переменные для разных окружений?

**О:** Используйте разные .env файлы:

```bash
# Staging
docker run --env-file .env.staging store-new

# Production
docker run --env-file .env.production store-new
```

## 🔗 Связанные документы

- [ISR Build Fix](./ISR_BUILD_FIX.md) - Почему не нужны build-time переменные
- [Environment Setup](./ENVIRONMENT_SETUP.md) - Настройка переменных окружения
- [CI/CD Recommendations](./CI_CD_RECOMMENDATIONS.md) - Настройка CI/CD

---

**Дата создания:** 2025-11-18  
**Статус:** ✅ Оптимизировано

