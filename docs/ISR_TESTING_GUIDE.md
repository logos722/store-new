# Тестирование исправления ISR проблемы

## 🎯 Цель

Убедиться, что исправление работает как локально, так и в Docker окружении.

## 🧪 Тест 1: Локальная разработка

### Шаг 1: Подготовка окружения

```bash
# Убедитесь, что backend запущен
curl http://localhost:5000/api/categories

# Если не запущен - запустите его
```

### Шаг 2: Запустите dev сервер

```bash
npm run dev
```

### Шаг 3: Проверьте главную страницу

Откройте http://localhost:3000

**✅ Ожидаемый результат:**
- Главная страница загружается
- Отображаются витрины каталогов
- Нет ошибок в консоли браузера
- Нет ошибок в терминале

**❌ Если проблема:**
```bash
# Проверьте логи в терминале
[fetchCatalogShowcase] Fetching: http://localhost:5000/api/catalog/...
[fetchCatalogShowcase] Success for ...: X products
```

---

## 🧪 Тест 2: Production Build локально

### Шаг 1: Создайте production build

```bash
npm run build
```

**✅ Ожидаемый результат билда:**

```
✓ Compiled successfully
✓ Linting and checking validity of types
✓ Collecting page data
✓ Generating static pages (X/X)
✓ Finalizing page optimization

Route (app)                              Size     First Load JS
┌ ƒ /                                    369 B           142 kB
...
ƒ  (Dynamic)  server-rendered on demand
```

**Важно:** Главная страница должна быть помечена как `ƒ (Dynamic)`, а НЕ как `○ (Static)`

**❌ Если видите ошибки ECONNREFUSED:**

Проверьте, что в `src/app/page.tsx` есть:
```typescript
export const dynamic = 'force-dynamic';
export const revalidate = 3600;
```

### Шаг 2: Запустите production сервер

```bash
npm run start
```

### Шаг 3: Проверьте результат

Откройте http://localhost:3000

**✅ Ожидаемый результат:**
- Главная страница отображается корректно
- Витрины каталогов загружены
- Проверьте логи в терминале:

```
[fetchCatalogShowcase] Fetching: http://localhost:5000/api/catalog/...
[fetchCatalogShowcase] Success for ...: X products
```

---

## 🧪 Тест 3: Docker Build (имитация проблемы)

### Шаг 1: Остановите backend

```bash
# Остановите ваш backend сервер
```

### Шаг 2: Попробуйте собрать Docker образ

```bash
docker build -t store-test .
```

**✅ Ожидаемый результат:**

Билд должен пройти **УСПЕШНО** без ошибок `ECONNREFUSED`, потому что:
- Главная страница использует `dynamic = 'force-dynamic'`
- Next.js НЕ пытается pre-render страницу во время билда
- API не нужен во время билда

**❌ До исправления было:**

```
Error fetching catalog showcase: TypeError: fetch failed
  cause: Error: connect ECONNREFUSED ::1:5000
```

**✅ После исправления:**

Билд проходит без ошибок API.

---

## 🧪 Тест 4: Docker запуск с backend

### Шаг 1: Запустите backend

```bash
# Запустите ваш backend сервер на порту 5000
```

### Шаг 2: Запустите Docker контейнер

```bash
docker run -d \
  --name store-test \
  -p 3001:3000 \
  -e API_BASE_URL=http://host.docker.internal:5000 \
  -e NEXT_PUBLIC_API_BASE_URL=http://localhost:5000 \
  store-test
```

**Note:** `host.docker.internal` позволяет Docker контейнеру обратиться к localhost хост-машины.

### Шаг 3: Проверьте логи

```bash
docker logs -f store-test
```

**✅ Ожидаемые логи:**

```
Ready - started server on 0.0.0.0:3000
[fetchCatalogShowcase] Fetching: http://host.docker.internal:5000/api/catalog/...
[fetchCatalogShowcase] Success for ...: X products
```

### Шаг 4: Проверьте в браузере

Откройте http://localhost:3001

**✅ Ожидаемый результат:**
- Главная страница отображается
- Витрины каталогов загружены

### Очистка

```bash
docker stop store-test
docker rm store-test
docker rmi store-test
```

---

## 🧪 Тест 5: Docker Compose (полное окружение)

### Шаг 1: Создайте docker-compose.test.yml

```yaml
version: '3.8'

services:
  backend:
    image: your-backend:latest
    container_name: test-backend
    ports:
      - "5001:5000"

  frontend:
    build: .
    container_name: test-frontend
    environment:
      - API_BASE_URL=http://backend:5000
      - NEXT_PUBLIC_API_BASE_URL=http://localhost:5001
      - NEXT_PUBLIC_BASE_URL=http://localhost:3002
    depends_on:
      - backend
    ports:
      - "3002:3000"
```

### Шаг 2: Запустите

```bash
docker-compose -f docker-compose.test.yml up -d
```

### Шаг 3: Проверьте логи

```bash
# Frontend логи
docker-compose -f docker-compose.test.yml logs -f frontend

# Backend логи
docker-compose -f docker-compose.test.yml logs -f backend
```

### Шаг 4: Проверьте в браузере

Откройте http://localhost:3002

**✅ Ожидаемый результат:**
- Главная страница отображается корректно
- Витрины каталогов загружены
- Изображения отображаются

### Очистка

```bash
docker-compose -f docker-compose.test.yml down
docker-compose -f docker-compose.test.yml down --volumes  # Если нужно удалить volumes
```

---

## 🧪 Тест 6: ISR кэширование

Этот тест проверяет, что ISR revalidation работает корректно.

### Шаг 1: Запустите production сервер

```bash
npm run build
npm run start
```

### Шаг 2: Первый запрос (без кэша)

```bash
time curl -s http://localhost:3000 > /dev/null
```

Засеките время (например, 1.5s)

### Шаг 3: Второй запрос (с кэшем)

```bash
time curl -s http://localhost:3000 > /dev/null
```

**✅ Ожидаемый результат:**
- Второй запрос значительно быстрее (например, 0.1s)
- Это означает, что ISR кэширование работает

### Шаг 4: Проверьте заголовки

```bash
curl -I http://localhost:3000
```

Ищите заголовок:
```
X-Nextjs-Cache: HIT
```

**Или** (для первого запроса):
```
X-Nextjs-Cache: MISS
```

---

## 📊 Чеклист тестирования

### Перед коммитом

- [ ] `npm run build` проходит без ошибок
- [ ] Локальный dev сервер работает корректно
- [ ] Production build работает локально
- [ ] Нет React warnings в консоли
- [ ] Нет TypeScript ошибок

### Перед деплоем

- [ ] Docker build проходит без ошибок ECONNREFUSED
- [ ] Docker контейнер запускается успешно
- [ ] Главная страница отображается в Docker
- [ ] Логи не содержат критических ошибок
- [ ] ISR кэширование работает

### На продакшене

- [ ] Главная страница загружается
- [ ] Витрины каталогов отображаются
- [ ] Изображения загружаются
- [ ] SEO метаданные на месте
- [ ] Нет 500 ошибок в логах
- [ ] Performance метрики приемлемы

---

## 🔍 Отладка

### Проблема: Билд падает с ECONNREFUSED

**Причина:** Не применено исправление.

**Решение:**
1. Проверьте `src/app/page.tsx`
2. Убедитесь, что есть `export const dynamic = 'force-dynamic'`
3. Пересоберите: `npm run build`

### Проблема: На проде пустые витрины

**Причина:** API недоступен во время рантайма.

**Решение:**
1. Проверьте переменные окружения:
```bash
docker exec -it <container> printenv | grep API
```

2. Проверьте доступность API:
```bash
docker exec -it <container> wget -O- $API_BASE_URL/api/categories
```

3. Проверьте логи:
```bash
docker logs <container> 2>&1 | grep fetchCatalogShowcase
```

### Проблема: ISR не кэширует

**Причина:** Возможно, используется dev сервер или неправильная конфигурация.

**Решение:**
1. Убедитесь, что запущен production сервер (`npm run start`, а не `npm run dev`)
2. Проверьте, что в `src/app/page.tsx` есть `export const revalidate = 3600`
3. Проверьте заголовки ответа на наличие `X-Nextjs-Cache`

---

## 📝 Отчет о тестировании

После прохождения всех тестов заполните:

```
Дата: ___________
Версия: _________

[ ] Тест 1: Локальная разработка - ✅ / ❌
[ ] Тест 2: Production Build - ✅ / ❌
[ ] Тест 3: Docker Build без API - ✅ / ❌
[ ] Тест 4: Docker с API - ✅ / ❌
[ ] Тест 5: Docker Compose - ✅ / ❌
[ ] Тест 6: ISR кэширование - ✅ / ❌

Комментарии:
_________________________________
_________________________________
```

---

## 🔗 Связанные документы

- [ISR_BUILD_FIX.md](./ISR_BUILD_FIX.md) - Описание проблемы и решения
- [ISR_ПРОБЛЕМА_РЕШЕНИЕ.md](./ISR_ПРОБЛЕМА_РЕШЕНИЕ.md) - Краткое решение (RU)
- [ENVIRONMENT_SETUP.md](./ENVIRONMENT_SETUP.md) - Настройка переменных окружения

---

**Дата создания:** 2025-11-18  
**Статус:** Готово к использованию

